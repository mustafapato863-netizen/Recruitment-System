# RecruitFlow M0 Exact-Source Baseline

Captured: 2026-08-23 16:34:26 +03:00  
Workspace: `D:\Projects\Recruitment Workflow System`  
Phase: M0 - Baseline truth and release discipline  
Status: Complete as an exact-source baseline; known product quality failures are carried into M1 and this is not a release signoff.

## Source identity

- Branch: `main`
- HEAD: `915b3a68158d53d485d3b3f7f76792634619a5e6`
- Root `AGENTS.md`: absent. The task-provided AGENTS instructions are the active policy for this run.
- Worktree policy: preserve all existing user-owned modifications and untracked artifacts; no reset, checkout, broad formatting, deletion, or branch creation was performed.
- Porcelain status manifest SHA-256: `dcdf63293e92b617056a5f7f09dce62c4975a3cdce2d704a23640ad19dfd2aef`

## Dirty-worktree inventory

At capture time, `git diff --name-only` reported 190 modified tracked paths, including one tracked deletion (`apps/web/src/styles/auth.css`), and `git ls-files --others --exclude-standard` reported 951 untracked paths. Changed-path counts by top-level root (tracked plus untracked) were:

| Root | Paths |
|---|---:|
| `.env.example` | 1 |
| `.github` | 1 |
| `apps` | 323 |
| `database` | 17 |
| `docs` | 73 |
| `findings.md` | 1 |
| `package.json` | 1 |
| `packages` | 3 |
| `pnpm-lock.yaml` | 1 |
| `pnpm-workspace.yaml` | 1 |
| `progress.md` | 1 |
| `scripts` | 9 |
| `task_plan.md` | 1 |
| `tasks.md` | 1 |
| `tests` | 707 |

The complete path lists remain authoritative in the exact-source commands below; the fingerprint above detects a changed worktree manifest without rewriting or staging user files.

## Runtime/process baseline

- PostgreSQL processes were present.
- Node processes were present, including listeners on `[::]:3000` and `[::]:3001`.
- A listener was present on `[::1]:5173`; its owning application must be identified before browser evidence is accepted.
- No listener was found on 3100, 4173, 5174, 5181, 5182, or 5185 at capture time.
- Existing processes are user-owned and were not stopped or restarted.

## Source inventory

| Area | Current count | Notes |
|---|---:|---|
| Web page modules | 39 | Lazy-routed React/Vite surfaces, including Talent Pool detail, Profile, Calendar, and protected design-system routes |
| Web shared UI TSX files | 70 | Local token/component system, overlays, tables, charts, workflow primitives, and tests |
| API controllers | 27 | NestJS route families with JWT and permission decorators |
| API services | 23 | Organization-aware domain services plus auth, search, reports, and infrastructure services |
| API DTOs | 20 | Class-validator request boundaries |
| Shared contract source files | 2 | `packages/contracts/src/index.ts` and notification/task contracts |
| Prisma migration directories | 8 | Through `20260823_auth_rate_limits`; migration status is still a required fresh gate |
| Database integration/security scripts | 9 | Local scripts; must run only against isolated test data |
| Browser/Python test files | 11 | Existing matrix and route-specific checks; fresh current-source execution required |
| CI workflow files | 1 | `.github/workflows/ci.yml`; deployment/IaC assets were not found in the repository inventory |

## Package/runtime map

- Root package manager: pnpm `11.9.0`; lockfile present.
- Web: React `19.2.8`, Vite `8.2.0`, route-level lazy loading, Vitest/jsdom component suite, Lucide/Radix/local primitives.
- API: NestJS `11.1.x`, Prisma client `6.19.3`, PostgreSQL, cookie/JWT auth, validation pipe, organization-scoped services.
- Worker: BullMQ and ioredis dependencies exist, but `apps/worker/src/main.ts` currently only logs a redacted Redis target; production queues/processors are not implemented.
- Database: PostgreSQL Prisma schema with eight migration directories and seed data; additive profile/preference and auth-rate-limit migrations are present in the dirty tree.
- Deployment: no Docker, Compose, Kubernetes, Terraform, Vercel, Netlify, or other deployment manifest was found in the current repository inventory.

## Exact reproduction commands

Run from the repository root:

```powershell
git branch --show-current
git rev-parse HEAD
git status --short
git diff --name-status
git ls-files --others --exclude-standard
Get-Process -Name node,pnpm,postgres,redis-server -ErrorAction SilentlyContinue
Get-NetTCPConnection -State Listen
```

The baseline fingerprint is for the status output captured with `git status --porcelain=v1` and UTF-8 SHA-256 hashing. It is not a substitute for reviewing the path lists before any overlapping file is edited.

## Fresh validation results - 2026-08-23 16:55 +03:00

| Check | Result | Evidence / classification |
|---|---|---|
| `npx --yes pnpm@11.9.0 install --frozen-lockfile` | PASS | Direct `pnpm` was unavailable on PATH; the pinned `npx` fallback completed successfully. |
| Prisma validate/status | PASS | Schema valid; all 8 migration directories applied to local `Recruitment_DB`. |
| Prisma generate | BLOCKED | Normal generation hit Windows `EPERM` replacing the locked engine. Same-schema Prisma 6.19.3 alternate output passed `SELECT 1`; generated JS/runtime files were restored to the ignored default output while preserving the locked engine. |
| Read-only relational audit | PASS, with script caveat | Zero reported orphan, cross-organization, duplicate, and headcount violations. Script prints `[PASS]` and exits 0 even for nonzero metrics; fail-closed behavior is an M1 item. |
| Lint | FAIL | 13 current-source errors across Talent Pool, Dashboard, resume parser, design-system preset, and API service files. |
| Typecheck | PASS | Workspace typecheck completed without errors. |
| Design tokens | PASS | Strict token validation passed; legacy utility count remains within the configured limit. |
| Vitest serial / 8 workers / 256 MB heap | PASS | 13 files and 25 tests in each correctly rooted run; historical memory failure not reproduced. |
| Production build | FAIL | API and worker build; web fails on icon, context, PageFrame, Button, FormField, and Talent Pool type-contract errors. |
| Bundle budget | FAIL | CSS 246.49 KB > 225 KB; JS 378.17 KB > 300 KB. PDF parser chunks are code-split. |
| `pnpm audit --prod` | PASS | No known vulnerabilities. |
| `git diff --check` | FAIL | Existing trailing whitespace in Login/Talent Pool files plus LF/CRLF normalization warnings. |
| Browser full matrix | FAIL | Extended run completed all 396 checks: 354 PASS / 42 FAIL. Failures are axe serious `color-contrast` findings on Dashboard, Candidates, Applications/Pipeline, and Reports. |
| Browser root probe | FAIL | 12 checks: 2 pass, 10 fail due serious `color-contrast` findings on the current Dashboard at the required widths/themes. |

Configuration review (names only) found 19 keys in `.env.example` and 16 in local `.env`; the local values were not printed. The documented JWT expiry keys use `*_EXPIRY`, while the auth service reads `*_EXPIRES_IN`. The three auth-limit keys are absent locally and use service defaults. Session/object-storage placeholders are documented but not consumed by the current runtime.

The browser run used the current Vite source on `http://127.0.0.1:5185` and the existing RecruitFlow API listener on `http://127.0.0.1:3000`. The original port-5173 process had no socket during this check; no user-owned process was stopped or restarted.

The failed browser entries did not report route assertion, overflow, console-error, or failed-response issues. The repeated WCAG finding is white text on action blue `#00a3e0` with a 2.87:1 contrast ratio; the 42 failures are therefore one shared token/component blocker repeated across the required widths and themes, not 42 independent route failures. Full artifacts: `tests/artifacts/m0-dual-mode-full-20260823`.

## M0 closure evidence

- [x] Frozen-lockfile dependency/install check executed and passed through the pinned package-manager fallback.
- [x] Prisma validate, generate attempt, migration status, and deploy checks executed; the locked-engine generation failure is classified and the client runtime was recovered without source changes.
- [x] Root lint, workspace typecheck, design-token validation, unit tests, production build, bundle budget, audit, and diff checks executed with exact failures recorded.
- [x] Read-only database integrity and negative security matrix executed; valid-write isolation/RBAC scripts were withheld from the shared local database and remain an isolated-test requirement.
- [x] Current-source browser matrix completed at 375, 430, 768, 1024, 1280, and 1440 widths in light and dark themes.
- [x] Historical full-test Node/Vitest memory failure reproduced by comparison and classified as not reproducible under current correct-root configurations.
- [x] Environment key review, P0 blocker classification, and active M1 entry order recorded in `task_plan.md`, `findings.md`, and `progress.md`.

M0 is closed as a truth-and-release-discipline phase. The known failing checks are deliberate M1/M2 entry conditions, not claims of production readiness.
