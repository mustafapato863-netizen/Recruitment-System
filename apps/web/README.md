# RecruitFlow Web

The React + Vite frontend for RecruitFlow.

The app consumes shared contracts, validation rules, and design-system components from the workspace packages. New screens should be organized by feature as the product grows.

Run it from the repository root with:

```bash
pnpm dev:web
```
